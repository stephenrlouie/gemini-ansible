---
# This playbook deploys a gemini master.

- hosts: gem-masters
  sudo: yes
  roles:
    - ansible-controller
    - dnsmasq
    - tftp
    - httpd
    - coreos-cloudinit
    - docker
  tags:
    - ansible-controller
    - dnsmasq
    - tftp
    - httpd
    - coreos-cloudinit
    - docker
